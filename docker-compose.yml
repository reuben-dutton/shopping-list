services:
  shopping-list:
    build: .
    volumes:
      - ./recipes:/app/recipes:ro
    restart: unless-stopped
    command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000", "--root-path", "/shopping"]
    networks:
      - caddy

networks:
  caddy:
    external: true